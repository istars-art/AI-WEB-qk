<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qk.management.mapper.DeptMapper">


    <insert id="insert">
        insert into dept(name,status,create_time,update_time) values(#{name},#{status},#{createTime},#{updateTime})
    </insert>
    <update id="update">
        UPDATE
            dept
        SET
            name = #{name},
            update_time = #{updateTime}
        WHERE
            id = #{id}
    </update>
    <select id="selectByCondition" resultType="com.qk.model.entity.Dept">
        SELECT
            *
        FROM
            dept
        <where>
            <if test="name != null">
                AND name LIKE concat('%',#{name},'%')
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
    </select>
    <select id="selectById" resultType="com.qk.model.entity.Dept">
        SELECT
            *
        FROM
            dept
        WHERE
            id = #{id}
    </select>

</mapper>